#include "main.h"

/**
 *mul- multiplies two numbers
 *@a: one of the numbers to be multiplied
 *@b: one of the numbers to be multiplied
 *
 *Return: the factor of the multiplication
 */

int mul(int a, int b)
{
	int factor;

	factor = a * b;
	return (factor);
}
