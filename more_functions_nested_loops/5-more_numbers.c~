#include "main.h"
#include <stdio.h>

/**
 *more_numbers- prints 0 through 14, inclusive, 10 times each on a new line
 *
 *Return: 0
 */

void more_numbers(void)
{
	int count;

	for (count = 0; count <= 10; count++)
	{
		printf("01234567891011121314\n");
	}
}
