#include <stdio.h>
/**
 *main- entrypoint
 *
 *Return: 0(always success
 */
int main(void)
{
	/**
	 *prints the specified string exactly
	 */
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
