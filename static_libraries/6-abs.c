#include "main.h"
#include <stdlib.h>

/**
 *_abs- calculates the absolute value of an input
 *@n:the number to perform the calculations on
 *
 *Return: absolute value of n
 */
int _abs(int n)
{
	n = abs(n);
	return (n);
}
